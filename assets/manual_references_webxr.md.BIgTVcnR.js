import{_ as t,c as o,o as s,a5 as a}from"./chunks/framework.DYgWcJ6Z.js";const u=JSON.parse('{"title":"WebXR basis","description":"","frontmatter":{},"headers":[],"relativePath":"manual/references/webxr.md","filePath":"manual/references/webxr.md","lastUpdated":1754235051000}'),r={name:"manual/references/webxr.md"};function n(i,e,c,l,h,d){return s(),o("div",null,e[0]||(e[0]=[a('<h1 id="webxr-basis" tabindex="-1">WebXR basis <a class="header-anchor" href="#webxr-basis" aria-label="Permalink to &quot;WebXR basis&quot;">​</a></h1><p>The <a href="https://developer.mozilla.org/en-US/docs/Web/API/WebXR_Device_API" target="_blank" rel="noreferrer">WebXR Device API</a> defines the core of the WebXR feature set, managing the selection of output devices, render the 3D scene to the chosen device at the appropriate frame rate, and manage motion vectors created using input controllers.</p><p>This reference provides the JSAR&#39;s implementation details of the basis of WebXR Device API.</p><h2 id="convention" tabindex="-1">Convention <a class="header-anchor" href="#convention" aria-label="Permalink to &quot;Convention&quot;">​</a></h2><p>In this document, we will use the following convention to describe implementation status:</p><table tabindex="0"><thead><tr><th>Code</th><th>Description</th></tr></thead><tbody><tr><td><em>NI</em></td><td><b>N</b>ot <b>I</b>mplemented</td></tr><tr><td><em>PI</em></td><td><b>P</b>artially <b>I</b>mplemented</td></tr><tr><td><em>YI</em></td><td><b>Y</b>et to be <b>I</b>mplemented</td></tr></tbody></table><h2 id="initialization" tabindex="-1">Initialization <a class="header-anchor" href="#initialization" aria-label="Permalink to &quot;Initialization&quot;">​</a></h2><p>The initialization of the WebXR device is achieved by the user agent, and the developer only needs to use the initialized <code>XRSystem</code> instance <code>navigator.xr</code>.</p><h3 id="class-xrsystem" tabindex="-1">Class <code>XRSystem</code> <a class="header-anchor" href="#class-xrsystem" aria-label="Permalink to &quot;Class `XRSystem`&quot;">​</a></h3><p>The <a href="https://developer.mozilla.org/en-US/docs/Web/API/WebXR_Device_API" target="_blank" rel="noreferrer">WebXR Device API</a> interface <code>XRSystem</code> provides methods which let you get access to an <code>XRSession</code> object representing a WebXR session. With that object, you can use it to interact with the XR device.</p><h3 id="instance-methods" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h3><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSystem/isSessionSupported" target="_blank" rel="noreferrer"><code>isSessionSupported()</code></a></strong></p><blockquote><p>Returns a promise which resolves to <code>true</code> if the user agent supports the given session mode. Resolves to <code>false</code> if the specified mode isn&#39;t supported.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSystem/requestSession" target="_blank" rel="noreferrer"><code>requestSession()</code></a></strong></p><blockquote><p>Returns a promise that resolves to a new <code>XRSession</code> with the specified session mode.</p></blockquote><h3 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h3><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSystem/devicechange_event" target="_blank" rel="noreferrer"><code>devicechange</code></a></strong> (<em>NI</em>)</p><blockquote><p>Sent when the set of available XR devices has changed. Also available using the <code>ondevicechange</code> event handler.</p></blockquote><h2 id="session" tabindex="-1">Session <a class="header-anchor" href="#session" aria-label="Permalink to &quot;Session&quot;">​</a></h2><p>A WebXR session represents the connection between the user agent and the XR device. It is created by calling <code>navigator.xr.requestSession()</code>.</p><h3 id="class-xrsession" tabindex="-1">Class <code>XRSession</code> <a class="header-anchor" href="#class-xrsession" aria-label="Permalink to &quot;Class `XRSession`&quot;">​</a></h3><h4 id="instance-properties" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/depthDataFormat" target="_blank" rel="noreferrer"><code>depthDataFormat</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns the depth-sensing data format with which the session was configured.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/depthUsage" target="_blank" rel="noreferrer"><code>depthUsage</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns the depth-sensing usage with which the session was configured.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/domOverlayState" target="_blank" rel="noreferrer"><code>domOverlayState</code></a></strong> (<em>NI</em>)</p><blockquote><p>Provides information about the DOM overlay, if the feature is enabled.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/environmentBlendMode" target="_blank" rel="noreferrer"><code>environmentBlendMode</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns this session&#39;s blend mode which denotes how much of the real-world environment is visible through the XR device and how the device will blend the device imagery with it.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/inputSources" target="_blank" rel="noreferrer"><code>inputSources</code></a></strong></p><blockquote><p>Returns a list of this session&#39;s <code>XRInputSource</code>s, each representing an input device used to control the camera and/or scene.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/interactionMode" target="_blank" rel="noreferrer"><code>interactionMode</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns this session&#39;s interaction mode, which describes the best space (according to the user agent) for the application to draw interactive UI for the current session.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/preferredReflectionFormat" target="_blank" rel="noreferrer"><code>preferredReflectionFormat</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns this session&#39;s preferred reflection format used for lighting estimation texture data.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/renderState" target="_blank" rel="noreferrer"><code>renderState</code></a></strong></p><blockquote><p>An <code>XRRenderState</code> object which contains options affecting how the imagery is rendered. This includes things such as the near and far clipping planes (distances defining how close and how far away objects can be and still get rendered), as well as field of view information.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/visibilityState" target="_blank" rel="noreferrer"><code>visibilityState</code></a></strong> (<em>NI</em>)</p><blockquote><p>A string indicating whether or not the session&#39;s imagery is visible to the user, and if so, if it&#39;s being visible but not currently the target for user events.</p></blockquote><h4 id="instance-methods-1" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods-1" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/end" target="_blank" rel="noreferrer"><code>end()</code></a></strong></p><blockquote><p>Ends the WebXR session. Returns a promise which resolves when the session has been successfully ended.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/cancelAnimationFrame" target="_blank" rel="noreferrer"><code>cancelAnimationFrame()</code></a></strong></p><blockquote><p>Removes a callback from the animation frame painting callback from <code>XRSession</code>&#39;s set of animation frame rendering callbacks, given the identifying handle returned by a previous call to <code>requestAnimationFrame()</code>.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/requestAnimationFrame" target="_blank" rel="noreferrer"><code>requestAnimationFrame()</code></a></strong></p><blockquote><p>Schedules the specified method to be called the next time the user agent is working on rendering an animation frame for the WebXR device. Returns an integer value which can be used to identify the request for the purposes of canceling the callback using <code>cancelAnimationFrame()</code>. This method is comparable to the <code>Window.requestAnimationFrame()</code> method.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/requestHitTestSource" target="_blank" rel="noreferrer"><code>requestHitTestSource()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Requests an <code>XRHitTestSource</code> object that handles hit test subscription.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/requestHitTestSourceForTransientInput" target="_blank" rel="noreferrer"><code>requestHitTestSourceForTransientInput()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Requests an <code>XRTransientInputHitTestSource</code> object that handles hit test subscription for a transient input source.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/requestLightProbe" target="_blank" rel="noreferrer"><code>requestLightProbe()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Requests an <code>XRLightProbe</code> object that handles light probe subscription.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/requestReferenceSpace" target="_blank" rel="noreferrer"><code>requestReferenceSpace()</code></a></strong></p><blockquote><p>Requests that a new <code>XRReferenceSpace</code> of the specified type be created. Returns a promise which resolves with the <code>XRReferenceSpace</code> or <code>XRBoundedReferenceSpace</code> which was requested, or throws a <code>NotSupportedError</code> <code>DOMException</code> if the requested space type isn&#39;t supported by the device.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/updateRenderState" target="_blank" rel="noreferrer"><code>updateRenderState()</code></a></strong></p><blockquote><p>Updates the properties of the session&#39;s render state.</p></blockquote><p><strong><code>updateCollisionBox(min: number[], max: number[]): void</code></strong></p><p>Update the collision box for the session, the collision box is used to do the hit testing in the host.</p><p>In JSAR&#39;s <strong>Defferred Composition</strong>, we leverage the hit testing to host to decrease the latency when user interacts with the virtual objects or the UI elements. With this method, the client(application) process does not need to do hit testing in every frame and avoid the latency from the client to the host.</p><p>This method <code>updateCollisionBox</code> is used to tell the host the collision box of a virtual object, then the host will do the hit testing to compute the hit point by itself.</p><ul><li><code>min</code> The minimum point of the collision box.</li><li><code>max</code> The maximum point of the collision box.</li></ul><h4 id="events-1" tabindex="-1">Events <a class="header-anchor" href="#events-1" aria-label="Permalink to &quot;Events&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/end_event" target="_blank" rel="noreferrer"><code>end</code></a></strong></p><blockquote><p>Sent to the <code>XRSession</code> object after the WebXR session has ended and all hardware-related functions have completed. The event is represented by an object of type <code>XRSessionEvent</code>. Also available through the onend event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/inputsourceschange_event" target="_blank" rel="noreferrer"><code>inputsourceschange</code></a></strong></p><blockquote><p>An event of type <code>XRInputSourcesChangeEvent</code> sent to the <code>XRSession</code> when the list of active XR input sources has changed. Also available through the <code>oninputsourceschange</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/select_event" target="_blank" rel="noreferrer"><code>select</code></a></strong></p><blockquote><p>An event of type <code>XRInputSourceEvent</code> which is sent to the session when one of the session&#39;s input sources has successfully completed a primary action. This generally corresponds to the user pressing a trigger, touchpad, or button, speaks a command, or performs a recognizable gesture. The <code>select</code> event is sent after the <code>selectstart</code> event is sent and immediately before the <code>selectend</code> event is sent. If <code>select</code> is not sent, then the select action was aborted before being completed. Also available through the <code>onselect</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/selectend_event" target="_blank" rel="noreferrer"><code>selectend</code></a></strong></p><blockquote><p>An event of type <code>XRInputSourceEvent</code> which gets sent to the session object when one of its input devices finishes its primary action or gets disconnected while in the process of handling a primary action. For example: for button or trigger actions, this means the button has been released; for spoken commands, it means the user has finished speaking. This is the last of the three <code>select*</code> events to be sent. Also available through the <code>onselectend</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/selectstart_event" target="_blank" rel="noreferrer"><code>selectstart</code></a></strong></p><blockquote><p>An event of type <code>XRInputSourceEvent</code> which is sent to the session object when one of its input devices is first engaged by the user in such a way as to cause the primary action to begin. This is the first of the <code>session*</code> event to be sent. Also available through the <code>onselectstart</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/squeeze_event" target="_blank" rel="noreferrer"><code>squeeze</code></a></strong></p><blockquote><p>An <code>XRInputSourceEvent</code> sent to indicate that a primary squeeze action has successfully completed. This indicates that the device being squeezed has been released, and may represent dropping a grabbed object, for example. It is sent immediately before the <code>squeezeend</code> event is sent to indicate that the squeeze action is over. Also available through the <code>onsqueeze</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/squeezestart_event" target="_blank" rel="noreferrer"><code>squeezestart</code></a></strong></p><blockquote><p>An event of type <code>XRInputSourceEvent</code> which is sent to the <code>XRSession</code> when the user initially squeezes a squeezable controller. This may be, for example, a trigger which is used to represent grabbing objects, or might represent actual squeezing when wearing a haptic glove. Also available through the <code>onsqueezestart</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/squeezeend_event" target="_blank" rel="noreferrer"><code>squeezeend</code></a></strong></p><blockquote><p>An <code>XRInputSourceEvent</code> sent to the <code>XRSession</code> when the primary squeeze action ends, whether or not the action was successful. Also available using the <code>onsqueezeend</code> event handler property.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRSession/visibilitychange_event" target="_blank" rel="noreferrer"><code>visibilitychange</code></a></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRSessionEvent</code> which is sent to the session when its visibility state as indicated by the <code>visibilityState</code> changes. Also available through the <code>onvisibilitychange</code> event handler property.</p></blockquote><h3 id="class-xrrenderstate" tabindex="-1">Class <code>XRRenderState</code> <a class="header-anchor" href="#class-xrrenderstate" aria-label="Permalink to &quot;Class `XRRenderState`&quot;">​</a></h3><h4 id="instance-properties-1" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-1" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState/baseLayer" target="_blank" rel="noreferrer"><code>baseLayer</code></a></strong></p><blockquote><p>The <code>XRWebGLLayer</code> from which the user agent&#39;s compositing system obtains the image for the XR session.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState/depthFar" target="_blank" rel="noreferrer"><code>depthFar</code></a></strong> (<em>NI</em>)</p><blockquote><p>The distance, in meters, of the far clip plane from the viewer. The far clip plane is the plane which is parallel to the display beyond which rendering of the scene no longer takes place. This, essentially, specifies the maximum distance the user can see.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState/depthNear" target="_blank" rel="noreferrer"><code>depthNear</code></a></strong> (<em>NI</em>)</p><blockquote><p>The distance, in meters, of the near clip plane from the viewer. The near clip plane is the plane, parallel to the display, at which rendering of the scene begins. Any closer to the viewer than this, and no portions of the scene are drawn.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState/inlineVerticalFieldOfView" target="_blank" rel="noreferrer"><code>inlineVerticalFieldOfView</code></a></strong> (<em>NI</em>)</p><blockquote><p>The default vertical field of view, defined in radians, to use when the session is in inline mode. null for all immersive sessions.</p></blockquote><p>JSAR won&#39;t support the inline mode, so this property would never be implemented.</p><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRenderState/layers" target="_blank" rel="noreferrer"><code>layers</code></a></strong> (<em>NI</em>)</p><blockquote><p>An array of <code>XRLayer</code> objects which define the layers to be composited into the final image.</p></blockquote><h2 id="frame-loop" tabindex="-1">Frame loop <a class="header-anchor" href="#frame-loop" aria-label="Permalink to &quot;Frame loop&quot;">​</a></h2><p>The frame loop classes are used to manage the rendering of the scene to the XR device.</p><h3 id="class-xrframe" tabindex="-1">Class <code>XRFrame</code> <a class="header-anchor" href="#class-xrframe" aria-label="Permalink to &quot;Class `XRFrame`&quot;">​</a></h3><h4 id="instance-properties-2" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-2" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/session" target="_blank" rel="noreferrer"><code>session</code></a></strong></p><blockquote><p>The <code>XRSession</code> that for which this <code>XRFrame</code> describes the tracking details for all objects. The information about a specific object can be obtained by calling one of the methods on the object.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/trackedAnchors" target="_blank" rel="noreferrer"><code>trackedAnchors</code></a></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRAnchorSet</code> containing all anchors still tracked in the frame.</p></blockquote><h4 id="instance-methods-2" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods-2" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/createAnchor" target="_blank" rel="noreferrer"><code>createAnchor()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns a promise which resolves to a free-floating <code>XRAnchor</code> object.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/fillJointRadii" target="_blank" rel="noreferrer"><code>fillJointRadii()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Populates a <code>Float32Array</code> with radii for a list of hand joint spaces. Returns <code>true</code> if successful for all spaces.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/fillPoses" target="_blank" rel="noreferrer"><code>fillPoses()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Populates a <code>Float32Array</code> with the matrices of the poses, relative to a given base space. Returns <code>true</code> if all spaces have a valid pose.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getDepthInformation" target="_blank" rel="noreferrer"><code>getDepthInformation()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns an <code>XRCPUDepthInformation</code> object containing CPU depth information for the frame.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getHitTestResults" target="_blank" rel="noreferrer"><code>getHitTestResults()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns an array of <code>XRHitTestResult</code> objects containing hit test results for a given <code>XRHitTestSource</code>.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getHitTestResultsForTransientInput" target="_blank" rel="noreferrer"><code>getHitTestResultsForTransientInput()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns an array of <code>XRTransientInputHitTestResult</code> objects containing hit test results for a given <code>XRTransientInputHitTestSource</code>.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getJointPose" target="_blank" rel="noreferrer"><code>getJointPose()</code></a></strong></p><blockquote><p>Returns an <code>XRJointPose</code> object providing the pose of a hand joint relative to a given base space.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getLightEstimate" target="_blank" rel="noreferrer"><code>getLightEstimate()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns an <code>XRLightEstimate</code> object containing information about the lighting conditions in the environment.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getPose" target="_blank" rel="noreferrer"><code>getPose()</code></a></strong></p><blockquote><p>Returns an <code>XRPose</code> object providing the pose of a space relative to a given base space.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRFrame/getViewerPose" target="_blank" rel="noreferrer"><code>getViewerPose()</code></a></strong></p><blockquote><p>Returns an <code>XRViewerPose</code> object providing the pose of the viewer&#39;s head relative to a given base space.</p></blockquote><h2 id="spaces" tabindex="-1">Spaces <a class="header-anchor" href="#spaces" aria-label="Permalink to &quot;Spaces&quot;">​</a></h2><p>WebXR spaces are used to represent the pose of objects in the 3D scene, such as the viewer, hands and controllers.</p><p>Kinds of spaces are inherited from the <code>XRSpace</code> class.</p><h3 id="class-xrspace" tabindex="-1">Class <code>XRSpace</code> <a class="header-anchor" href="#class-xrspace" aria-label="Permalink to &quot;Class `XRSpace`&quot;">​</a></h3><p>No defined properties or methods.</p><h3 id="class-xrreferencespace" tabindex="-1">Class <code>XRReferenceSpace</code> <a class="header-anchor" href="#class-xrreferencespace" aria-label="Permalink to &quot;Class `XRReferenceSpace`&quot;">​</a></h3><blockquote><p>Represents a reference space which is typically expected to remain static for the duration of the <code>XRSession</code>. While objects may move within the space, the space itself remains fixed in place. There are exceptions to this static nature; most commonly, an <code>XRReferenceSpace</code> may move in order to adjust based on reconfiguration of the user&#39;s headset or other motion-sensitive device.</p></blockquote><p>To create a reference space, you need to call the <code>XRSession.requestReferenceSpace()</code> method.</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">xrSession.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">requestReferenceSpace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;local&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">referenceSpace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Use the reference space</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h4 id="instance-methods-3" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods-3" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRReferenceSpace/getOffsetReferenceSpace" target="_blank" rel="noreferrer"><code>getOffsetReferenceSpace()</code></a></strong></p><blockquote><p>Creates and returns a new reference space object as the same type as the one on which you call the method (so, either <code>XRReferenceSpace</code> or <code>XRBoundedReferenceSpace</code>). The new reference space can be used to transform a coordinate from the reference space of the object on which the method is called into a different coordinate space. This is useful for positioning objects while rendering, and to perform the needed transforms when changing the viewer&#39;s position and/or orientation in 3D space.</p></blockquote><h4 id="events-2" tabindex="-1">Events <a class="header-anchor" href="#events-2" aria-label="Permalink to &quot;Events&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRReferenceSpace/reset_event" target="_blank" rel="noreferrer"><code>reset</code></a></strong> (<em>NI</em>)</p><blockquote><p>Sent to the <code>XRReferenceSpace</code> object when the reference space is reset. This can happen when the user reconfigures their headset or other motion-sensitive device, causing the reference space to be redefined. Also available through the <code>onreset</code> event handler property.</p></blockquote><h4 id="reference-space-types" tabindex="-1">Reference space types <a class="header-anchor" href="#reference-space-types" aria-label="Permalink to &quot;Reference space types&quot;">​</a></h4><p><strong><code>bounded-floor</code></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRBoundedReferenceSpace</code> similar to the <code>local</code> type, except the user is not expected to move outside a predetermined boundary, given by the <code>boundsGeometry</code> in the returned object.</p></blockquote><p><strong><code>local</code></strong></p><blockquote><p>An <code>XRReferenceSpace</code> tracking space whose native origin is located near the viewer&#39;s position at the time the session was created. The exact position depends on the underlying platform and implementation. The user isn&#39;t expected to move much if at all beyond their starting position, and tracking is optimized for this use case. For devices with six degrees of freedom (6DoF) tracking, the <code>local</code> reference space tries to keep the origin stable relative to the environment.</p></blockquote><p><strong><code>local-floor</code></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRReferenceSpace</code> similar to the <code>local</code> type, except the starting position is placed in a safe location for the viewer to stand, where the value of the y axis is 0 at floor level. If that floor level isn&#39;t known, the user agent will estimate the floor level. If the estimated floor level is non-zero, the browser is expected to round it such a way as to avoid fingerprinting (likely to the nearest centimeter).</p></blockquote><p><strong><code>unbounded</code></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRReferenceSpace</code> tracking space which allows the user total freedom of movement, possibly over extremely long distances from their origin point. The viewer isn&#39;t tracked at all; tracking is optimized for stability around the user&#39;s current position, so the native origin may drift as needed to accommodate that need.</p></blockquote><p><strong><code>viewer</code></strong> (<em>NI</em>)</p><blockquote><p>An <code>XRReferenceSpace</code> tracking space whose native origin tracks the viewer&#39;s position and orientation. This is used for environments in which the user can physically move around, and is supported by all instances of <code>XRSession</code>, both immersive and inline, though it&#39;s most useful for inline sessions. It&#39;s particularly useful when determining the distance between the viewer and an input, or when working with offset spaces. Otherwise, typically, one of the other reference space types will be used more often.</p></blockquote><h3 id="class-xrboundedreferencespace-ni" tabindex="-1">Class <code>XRBoundedReferenceSpace</code> (<em>NI</em>) <a class="header-anchor" href="#class-xrboundedreferencespace-ni" aria-label="Permalink to &quot;Class `XRBoundedReferenceSpace` (*NI*)&quot;">​</a></h3><blockquote><p>Represents a reference space which may move within a region of space whose borders are defined by an array of points laid out in clockwise order along the floor to define the passable region of the space. The origin of an <code>XRBoundedReferenceSpace</code> is always at floor level, with its X and Z coordinates typically defaulting to a location near the room&#39;s center.</p></blockquote><h3 id="class-xrjointspace" tabindex="-1">Class <code>XRJointSpace</code> <a class="header-anchor" href="#class-xrjointspace" aria-label="Permalink to &quot;Class `XRJointSpace`&quot;">​</a></h3><blockquote><p>Represents the space of an <code>XRHand</code> joint.</p></blockquote><p>You can use an <code>XRJointSpace</code> object and an <code>XRReferenceSpace</code> to get an <code>XRJointPose</code> by calling <code>XRFrame.getJointPose()</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">navigator.xr</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">requestSession</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ optionalFeatures: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;hand-tracking&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] })</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> renderFrame</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">session</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XRSession</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">frame</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XRFrame</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> inputSource</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> session.inputSources) {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (inputSource.hand) {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> indexFingerTip</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XRJointSpace</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inputSource.hand.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;index-finger-tip&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(indexFingerTip.jointName); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;index-finger-tip&#39;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> jointPose</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XRJointPose</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> frame.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getJointPose</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(indexFingerTip, referenceSpace);</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(jointPose.transform); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// XRRigidTransform</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h4 id="instance-properties-3" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-3" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRJointSpace/jointName" target="_blank" rel="noreferrer"><code>jointName</code></a></strong></p><blockquote><p>The name of the joint that is tracked.</p></blockquote><h2 id="views" tabindex="-1">Views <a class="header-anchor" href="#views" aria-label="Permalink to &quot;Views&quot;">​</a></h2><h3 id="class-xrview" tabindex="-1">Class <code>XRView</code> <a class="header-anchor" href="#class-xrview" aria-label="Permalink to &quot;Class `XRView`&quot;">​</a></h3><p>To draw everything the user sees, each frame requires iterating over the list of views returned by the <code>XRViewerPose</code> object&#39;s <code>views</code> list:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> view</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pose.views) {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> viewport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XRViewport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> glBaseLayer.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getViewport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(view);</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  gl.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">viewport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(viewport.x, viewport.y, viewport.width, viewport.height);</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Draw the scene for this view</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Or you can do this in multiview mode:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> view</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pose.views) {</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Update the view and projection matrices</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ...</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Draw the scene</span></span></code></pre></div><h4 id="instance-properties-4" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-4" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/eye" target="_blank" rel="noreferrer"><code>eye</code></a></strong></p><blockquote><p>Which of the two eyes (left) or (right) for which this XRView represents the perspective. This value is used to ensure that any content which is pre-rendered for presenting to a specific eye is distributed or positioned correctly. The value can also be none if the XRView is presenting monoscopic data (such as a 2D image, a fullscreen view of text, or a close-up view of something that doesn&#39;t need to appear in 3D).</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/isFirstPersonObserver" target="_blank" rel="noreferrer"><code>isFirstPersonObserver</code></a></strong> (<em>NI</em>)</p><blockquote><p>Returns a boolean indicating if the <code>XRView</code> is a first-person observer view.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/projectionMatrix" target="_blank" rel="noreferrer"><code>projectionMatrix</code></a></strong></p><blockquote><p>The projection matrix that will transform the scene to appear correctly given the point-of-view indicated by <code>eye</code>. This matrix should be used directly in order to avoid presentation distortions that may lead to potentially serious user discomfort.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/recommendedViewportScale" target="_blank" rel="noreferrer"><code>recommendedViewportScale</code></a></strong> (<em>NI</em>)</p><blockquote><p>The recommended viewport scale value that you can use for <code>requestViewportScale()</code> if the user agent has such a recommendation; <code>null</code> otherwise.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/transform" target="_blank" rel="noreferrer"><code>transform</code></a></strong></p><blockquote><p>An <code>XRRigidTransform</code> which describes the current position and orientation of the viewpoint in relation to the <code>XRReferenceSpace</code> specified when <code>getViewerPose()</code> was called on the <code>XRFrame</code> being rendered.</p></blockquote><h4 id="instance-methods-4" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods-4" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRView/requestViewportScale" target="_blank" rel="noreferrer"><code>requestViewportScale()</code></a></strong> (<em>NI</em>)</p><blockquote><p>Requests that the user agent should set the requested viewport scale for this viewport to the requested value.</p></blockquote><h3 id="class-xrviewport" tabindex="-1">Class <code>XRViewport</code> <a class="header-anchor" href="#class-xrviewport" aria-label="Permalink to &quot;Class `XRViewport`&quot;">​</a></h3><h4 id="instance-properties-5" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-5" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRViewport/height" target="_blank" rel="noreferrer"><code>height</code></a></strong></p><blockquote><p>The height, in pixels, of the viewport.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRViewport/width" target="_blank" rel="noreferrer"><code>width</code></a></strong></p><blockquote><p>The width, in pixels, of the viewport.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRViewport/x" target="_blank" rel="noreferrer"><code>x</code></a></strong></p><blockquote><p>The offset from the origin of the destination graphics surface (typically a <code>XRWebGLLayer</code>) to the left edge of the viewport, in pixels.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRViewport/y" target="_blank" rel="noreferrer"><code>y</code></a></strong></p><blockquote><p>The offset from the origin of the viewport to the bottom edge of the viewport; WebGL&#39;s coordinate system places (0, 0) at the bottom left corner of the surface.</p></blockquote><h2 id="geometric-primitives" tabindex="-1">Geometric primitives <a class="header-anchor" href="#geometric-primitives" aria-label="Permalink to &quot;Geometric primitives&quot;">​</a></h2><h3 id="class-xrrigidtransform" tabindex="-1">Class <code>XRRigidTransform</code> <a class="header-anchor" href="#class-xrrigidtransform" aria-label="Permalink to &quot;Class `XRRigidTransform`&quot;">​</a></h3><h4 id="constructor" tabindex="-1">Constructor <a class="header-anchor" href="#constructor" aria-label="Permalink to &quot;Constructor&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRigidTransform/XRRigidTransform" target="_blank" rel="noreferrer"><code>XRRigidTransform()</code></a></strong></p><blockquote><p>Creates a new XRRigidTransform object which represents a transform that applies a specified position and/or orientation.</p></blockquote><h4 id="instance-properties-6" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-6" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRigidTransform/position" target="_blank" rel="noreferrer"><code>position</code></a></strong></p><blockquote><p>A <code>DOMPointReadOnly</code> specifying a 3-dimensional point, expressed in meters, describing the translation component of the transform. The <code>w</code> property is always 1.0.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRigidTransform/orientation" target="_blank" rel="noreferrer"><code>orientation</code></a></strong></p><blockquote><p>A <code>DOMPointReadOnly</code> which contains a unit quaternion describing the rotational component of the transform. As a unit quaternion, its length is always normalized to 1.0.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRigidTransform/matrix" target="_blank" rel="noreferrer"><code>matrix</code></a></strong></p><blockquote><p>Returns the transform matrix in the form of a 16-member <code>Float32Array</code>.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRRigidTransform/inverse" target="_blank" rel="noreferrer"><code>inverse</code></a></strong></p><blockquote><p>Returns a <code>XRRigidTransform</code> which is the inverse of this transform. That is, if applied to an object that had been previously transformed by the original transform, it will undo the transform and return the original object.</p></blockquote><h2 id="pose" tabindex="-1">Pose <a class="header-anchor" href="#pose" aria-label="Permalink to &quot;Pose&quot;">​</a></h2><h3 id="class-xrpose" tabindex="-1">Class <code>XRPose</code> <a class="header-anchor" href="#class-xrpose" aria-label="Permalink to &quot;Class `XRPose`&quot;">​</a></h3><h4 id="instance-properties-7" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-7" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRPose/angularVelocity" target="_blank" rel="noreferrer"><code>angularVelocity</code></a></strong> (<em>NI</em>)</p><blockquote><p>A <code>DOMPointReadOnly</code> object which specifies the angular velocity of the object being tracked, expressed in radians per second.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRPose/emulatedPosition" target="_blank" rel="noreferrer"><code>emulatedPosition</code></a></strong></p><blockquote><p>A Boolean value which is <code>false</code> if the position and orientation given by <code>transform</code> is obtained directly from a full six degree of freedom (6DoF) XR device (that is, a device which tracks not only the pitch, yaw, and roll of the head but also the forward, backward, and side-to-side motion of the viewer). If any component of the transform is computed or created artificially (such as by using mouse or keyboard controls to move through space), this value is instead <code>true</code>, indicating that the <code>transform</code> is in part emulated in software.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRPose/linearVelocity" target="_blank" rel="noreferrer"><code>linearVelocity</code></a></strong> (<em>NI</em>)</p><blockquote><p>A <code>DOMPointReadOnly</code> object which specifies the linear velocity of the object being tracked, expressed in meters per second.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRPose/transform" target="_blank" rel="noreferrer"><code>transform</code></a></strong></p><blockquote><p>A <code>XRRigidTransform</code> which provides the position and orientation of the pose relative to the base <code>XRSpace</code>.</p></blockquote><h3 id="class-xrjointpose" tabindex="-1">Class <code>XRJointPose</code> <a class="header-anchor" href="#class-xrjointpose" aria-label="Permalink to &quot;Class `XRJointPose`&quot;">​</a></h3><p>The <code>XRJointPose</code> interface is an <code>XRPose</code> with additional information about the size of the skeleton joint it represents.</p><h4 id="instance-properties-8" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-8" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRJointPose/radius" target="_blank" rel="noreferrer"><code>radius</code></a></strong></p><blockquote><p>The radius (distance from skin) for a joint.</p></blockquote><h3 id="class-xrviewerpose" tabindex="-1">Class <code>XRViewerPose</code> <a class="header-anchor" href="#class-xrviewerpose" aria-label="Permalink to &quot;Class `XRViewerPose`&quot;">​</a></h3><p>The <a href="https://developer.mozilla.org/en-US/docs/Web/API/WebXR_Device_API" target="_blank" rel="noreferrer">WebXR Device API</a> interface <code>XRViewerPose</code> represents the pose (the position and orientation) of a viewer&#39;s point of view on the scene. Each <code>XRViewerPose</code> can have multiple views to represent, for example, the slight separation between the left and right eye.</p><h4 id="instance-properties-9" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-9" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRViewerPose/views" target="_blank" rel="noreferrer"><code>views</code></a></strong></p><blockquote><p>An array of <code>XRView</code> objects, one for each viewpoint on the scene which is needed to represent the scene to the user. A typical headset provides a viewer pose with two views whose <code>eye</code> property is either <code>left</code> or <code>right</code>, indicating which eye that view represents. Taken together, these views can reproduce the 3D effect when displayed on the XR device.</p></blockquote><h2 id="input" tabindex="-1">Input <a class="header-anchor" href="#input" aria-label="Permalink to &quot;Input&quot;">​</a></h2><h3 id="class-xrhand" tabindex="-1">Class <code>XRHand</code> <a class="header-anchor" href="#class-xrhand" aria-label="Permalink to &quot;Class `XRHand`&quot;">​</a></h3><h3 id="class-xrinputsource" tabindex="-1">Class <code>XRInputSource</code> <a class="header-anchor" href="#class-xrinputsource" aria-label="Permalink to &quot;Class `XRInputSource`&quot;">​</a></h3><h3 id="class-xrinputsourcearray" tabindex="-1">Class <code>XRInputSourceArray</code> <a class="header-anchor" href="#class-xrinputsourcearray" aria-label="Permalink to &quot;Class `XRInputSourceArray`&quot;">​</a></h3><h2 id="layers" tabindex="-1">Layers <a class="header-anchor" href="#layers" aria-label="Permalink to &quot;Layers&quot;">​</a></h2><h3 id="class-xrlayer" tabindex="-1">Class <code>XRLayer</code> <a class="header-anchor" href="#class-xrlayer" aria-label="Permalink to &quot;Class `XRLayer`&quot;">​</a></h3><p>This is an interface that represents a layer of content to be presented to the user, no properties and methods are defined.</p><h2 id="webgl-binding" tabindex="-1">WebGL binding <a class="header-anchor" href="#webgl-binding" aria-label="Permalink to &quot;WebGL binding&quot;">​</a></h2><h3 id="class-xrwebgllayer" tabindex="-1">Class <code>XRWebGLLayer</code> <a class="header-anchor" href="#class-xrwebgllayer" aria-label="Permalink to &quot;Class `XRWebGLLayer`&quot;">​</a></h3><h4 id="constructor-1" tabindex="-1">Constructor <a class="header-anchor" href="#constructor-1" aria-label="Permalink to &quot;Constructor&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/XRWebGLLayer" target="_blank" rel="noreferrer"><code>XRWebGLLayer()</code></a></strong></p><blockquote><p>Creates and returns a new <code>XRWebGLLayer</code> object for use by the specified <code>XRSession</code>, using a particular <code>WebGLRenderingContext</code> or <code>WebGL2RenderingContext</code> as the destination context.</p></blockquote><h4 id="instance-properties-10" tabindex="-1">Instance properties <a class="header-anchor" href="#instance-properties-10" aria-label="Permalink to &quot;Instance properties&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/antialias" target="_blank" rel="noreferrer"><code>antialias</code></a></strong></p><blockquote><p>A <code>Boolean</code> value indicating whether or not the WebGL context&#39;s framebuffer supports anti-aliasing. The specific type of anti-aliasing is determined by the user agent.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/fixedFoveation" target="_blank" rel="noreferrer"><code>fixedFoveation</code></a></strong> (<em>NI</em>)</p><blockquote><p>A number indicating the amount of foveation used by the XR compositor. Fixed Foveated Rendering (FFR) renders the edges of the eye textures at a lower resolution than the center and reduces the GPU load.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/framebuffer" target="_blank" rel="noreferrer"><code>framebuffer</code></a></strong> (<em>PI</em>)</p><blockquote><p>Returns a <code>WebGLFramebuffer</code> suitable for passing into the <code>bindFrameBuffer()</code> method.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/framebufferWidth" target="_blank" rel="noreferrer"><code>framebufferWidth</code></a></strong></p><blockquote><p>Returns the width of the <code>XRWebGLLayer</code>&#39;s framebuffer.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/framebufferHeight" target="_blank" rel="noreferrer"><code>framebufferHeight</code></a></strong></p><blockquote><p>Returns the height of the <code>XRWebGLLayer</code>&#39;s framebuffer.</p></blockquote><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/ignoreDepthValues" target="_blank" rel="noreferrer"><code>ignoreDepthValues</code></a></strong> (<em>NI</em>)</p><blockquote><p>A <code>Boolean</code> value indicating whether or not the depth values in the framebuffer should be ignored.</p></blockquote><p><strong><code>multiviewRequired</code></strong></p><p>This readonly property is introduced in JSAR to help WebXR developers to know if the framebuffer is multiview required.</p><p>At JSAR, the WebXR rendering pipeline disallows developers to create framebuffer objects for stereo rendering, because the framebuffer is always created by the host such as Unity, Unreal Engine, etc and the framebuffer is shared to the client process for rendering the WebXR content.</p><p>If the framebuffer is multiview required, namely calling <code>glFramebufferTextureMultiviewOVR()</code> in the host, the client process must use the followings in their vertex shader:</p><div class="language-glsl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">glsl</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#extension GL_OVR_multiview2 : enable</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">layout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(num_views </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Otherwise, any draw calls will occur an error by the <code>ovr_multiview*</code> extension.</p><p>To help WebXR developers to know if the framebuffer is multiview required, this property <code>multiviewRequired</code> is introduced in every created <code>XRWebGLLayer</code> object, and developers can check this property to know if the framebuffer is multiview required and change their shaders.</p><h4 id="static-methods" tabindex="-1">Static methods <a class="header-anchor" href="#static-methods" aria-label="Permalink to &quot;Static methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/getNativeFramebufferScaleFactor" target="_blank" rel="noreferrer"><code>getNativeFramebufferScaleFactor()</code></a></strong></p><blockquote><p>Returns the scaling factor that can be used to scale the resolution of the recommended WebGL framebuffer resolution to the rendering device&#39;s native resolution.</p></blockquote><h4 id="instance-methods-5" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods-5" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h4><p><strong><a href="https://developer.mozilla.org/en-US/docs/Web/API/XRWebGLLayer/getViewport" target="_blank" rel="noreferrer"><code>getViewport()</code></a></strong></p><blockquote><p>Returns a new <code>XRViewport</code> instance representing the position, width, and height to which the WebGL context&#39;s viewport must be set to contain drawing to the area of the framebuffer designated for the specified view&#39;s contents. In this way, for example, the rendering of the left eye&#39;s point of view and of the right eye&#39;s point of view are each placed into the correct parts of the framebuffer.</p></blockquote>',261)]))}const b=t(r,[["render",n]]);export{u as __pageData,b as default};
