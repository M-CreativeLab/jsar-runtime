import{_ as t,c as o,o as i,a5 as n}from"./chunks/framework.DYgWcJ6Z.js";const m=JSON.parse('{"title":"Defferred Composition","description":"","frontmatter":{},"headers":[],"relativePath":"contributing/defferred_composition.md","filePath":"contributing/defferred_composition.md","lastUpdated":1730705910000}'),a={name:"contributing/defferred_composition.md"};function r(s,e,c,h,l,d){return i(),o("div",null,e[0]||(e[0]=[n('<h1 id="defferred-composition" tabindex="-1">Defferred Composition <a class="header-anchor" href="#defferred-composition" aria-label="Permalink to &quot;Defferred Composition&quot;">​</a></h1><p>Defferred composition is a new technique introduced by JSAR that allows you to defer the composition of some of virtual objects in low latency scenarios. This is useful when you want to do composition of virtual objects from different processes or threads, and you also require low latency in this composition. JSAR uses defferred composition to compose virtual objects from WebXR processes into the host scene created by Unity, Unreal Engine or any other 3D engine.</p><h2 id="principles" tabindex="-1">Principles <a class="header-anchor" href="#principles" aria-label="Permalink to &quot;Principles&quot;">​</a></h2><p>Defferred composition must follow the principles below:</p><ul><li><strong>Low latency</strong>: The composition must be done in a very short time to avoid delays in the rendering of the host scene.</li><li><strong>Asynchronous</strong>: The composition must be done asynchronously to avoid blocking the main thread of the host scene.</li></ul><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>To achieve low latency and asynchronous composition, a defferred composition system must accept the client and host are in different time frames, namely the client must be defferred in time. That&#39;s why it&#39;s called <strong>Defferred Composition</strong>.</p><p>When we accept that the rendering in client-side is defferred, the host can render the scene without waiting for the client to send the virtual objects, instead, the host can render the whole scene and then compose the latest virtual objects from the client. This way, the host can render the scene with low latency and the client can send the virtual objects asynchronously.</p><p>Now there is only one issue left, how should we make defferred virtual objects appear in the right place when compositing? To solve this issue, we introduced <a href="https://www.cs.cornell.edu/courses/cs5740/2017sp/lectures/04-nn-compgraph.pdf" target="_blank" rel="noreferrer">computation graph</a> and a new variable type <code>Placeholder</code> in WebXR and renderer subsystem, that means the client will be able to receive a placeholder object from WebXR&#39;s API such as <code>frame.getViewerPose()</code> or <code>frame.getPose()</code>, and then the client could use this placeholder object to set the view-relative matrices of the virtual objects, once the host renderer receives a placeholder, it will use built-in computation graph to compute the right matrices for rendering, and then the virtual objects will appear in the right place.</p><h2 id="advantages" tabindex="-1">Advantages <a class="header-anchor" href="#advantages" aria-label="Permalink to &quot;Advantages&quot;">​</a></h2><p>In defferred composition, the host can render the scene with low latency and the client can send the virtual objects asynchronously. It&#39;s verified that defferred composition can achieve low latency and asynchronous composition in rendering WebXR applications within an Unity scene.</p><h2 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h2><p>Defferred composition has limitations as well even though it&#39;s working in JSAR.</p><p>The main limitation is that we have to introduce a new variable type <code>Placeholder</code> in WebXR Device API, and to implement the complete computation graph in current ECMAScript engines, which is not a trivial task, it requires a new Proxy way to handle the operations between placeholders and other variables.</p><p>Another limitation is that the WebXR developer must accept the fact that the rendering in client-side is defferred, that means all the poses and matrices are not precise in the client-side.</p>',15)]))}const f=t(a,[["render",r]]);export{m as __pageData,f as default};
